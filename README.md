# Image
To change the image, simply replace the `image.png` file for any other image, but it needs to be named `image.png`.

# Arguments
`-t`, `--test` shows the number of hits the image will take before hitting a corner.
